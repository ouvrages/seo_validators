en:
  validators:
    redundant_image_alt:
      message: "Redundant alt attribute. Screen-readers already announce `img` tags as an image. You don\'t need to use the word `image`, `photo,` or `picture` (or any specified custom words)"
